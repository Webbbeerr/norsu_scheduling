# Smart Scheduling System - Environment Configuration
# Local dev overrides go in .env.local (not committed to git)
# Production values are set via Railway environment variables

# Application
APP_ENV=prod
APP_SECRET=change_me_in_production

# Database Connection URL (overridden by Railway DATABASE_URL env var)
DATABASE_URL="mysql://user:password@localhost:3306/database?serverVersion=8.0"

# Mailer
MAILER_DSN=null://null

# Default URI
DEFAULT_URI=http://localhost:8000

# Messenger
MESSENGER_TRANSPORT_DSN=doctrine://default
